
# Increase the network timeout to prevent timeouts during installation
network-timeout=100000
# Disable audit to speed up installation
audit=false
# Set the registry explicitly
registry=https://registry.npmjs.org/
# Don't save exact versions
save-exact=false
